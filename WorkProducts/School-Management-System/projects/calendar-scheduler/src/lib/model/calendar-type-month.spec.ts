import { CalendarTypeMonth } from './calendar-type-month';

describe('CalendarTypeMonth', () => {
  it('should create an instance', () => {
    expect(new CalendarTypeMonth()).toBeTruthy();
  });
});
